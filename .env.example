# Public Supabase project URL. Safe to expose, but only used by server code in v1.
NEXT_PUBLIC_SUPABASE_URL=

# Server-only secret. Never expose this value to the browser or commit it.
SUPABASE_SERVICE_ROLE_KEY=

# At least 32 random characters used to hash request fingerprints for abuse control.
APPLICATION_HASH_SECRET=
